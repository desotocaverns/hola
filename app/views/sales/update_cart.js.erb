// Add js for nice notice (ticket has been added)
TicketStore.updateTicketForms("<%= escape_javascript render(partial: 'quantities', status: :success) %>")

$("#quick-cart")
  .html("<%= escape_javascript render(partial: 'quick_cart', status: :success) %>")
  .removeClass("show")
  .addClass("updated")

$("#edit-cart")
  .html("<%= escape_javascript render(partial: 'edit_cart') %>")
