<%= form_for @sale, :url => {action: "charge"}, :method => "patch", :html => {:id => "payment_form"} do |f| %>
  <input type="hidden" autocomplete="off" name="sale[stripe_token]" id="stripe_token"/>
  <%= f.hidden_field :token %>
<% end %>
